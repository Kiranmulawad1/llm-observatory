apiVersion: v1
kind: Service
metadata:
  name: lo-web
  labels:
    app.kubernetes.io/name: lo-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lo-web
  ports:
    - name: http
      port: 3000
      targetPort: http
